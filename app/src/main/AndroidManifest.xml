<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.conrep">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConRep">
        <activity android:name=".ViewTask"></activity>
        <activity android:name=".TaskList" />
        <activity android:name=".AddTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FileReport" />
        <activity
            android:name=".ViewConstructionSite"
            android:label="@string/title_activity_view_construction_site"
            android:theme="@style/Theme.ConRep.NoActionBar" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>